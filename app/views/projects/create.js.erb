$("<%= escape_javascript(render @project) %>").fadeIn(300).appendTo("#projects");
//$("#new_project").html('<%= link_to new_project_path , remote: true, :class => "btn btn-primary" do %>  <span class="glyphicon glyphicon-plus"></span> Add TODO List <% end %>');
$('#myModal').modal("hide");
